import { View, Text } from "react-native";

export default function Scene() {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text>This is scene screen.</Text>
    </View>
  );
}
