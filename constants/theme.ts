import { StyleSheet } from "react-native";

const wordCardStyles = StyleSheet.create({
  sectionBlock: {
    padding: 10,
    borderWidth: 1,
    borderRadius: 8,
  },
});

export { wordCardStyles };
